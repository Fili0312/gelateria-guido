# Installare in /etc/nginx/conf.d/: questa direttiva deve vivere nel contesto
# `http`, non dentro il blocco `server` che include gelateria.conf.
limit_req_zone $binary_remote_addr zone=gelateria_login:1m rate=6r/m;
