.next
node_modules
src/generated
prisma/migrations
pnpm-lock.yaml
next-env.d.ts
storage
tests/fixtures

# I documenti sono prosa formattata a mano: riformattarli a ogni `pnpm format`
# produce solo rumore nei diff.
docs
*.md

# `next build` riscrive tsconfig.json espandendo gli array. Se Prettier lo
# ricompatta, ogni build sporca il worktree e il deploy successivo si ferma
# sul controllo «albero pulito». Vince Next: è lui a scriverlo.
tsconfig.json
